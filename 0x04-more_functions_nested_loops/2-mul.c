/* this here is about multiplication*/
#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
* mul - this function multiply 2
* integer
* @a: means parameter is an int
* @b: means parameter is an int
* Return: product
*/
int mul(int a, int b)
{

	int product;

	product = (a * b);
	return (product);
}
