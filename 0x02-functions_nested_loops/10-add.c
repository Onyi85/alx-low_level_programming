/*
 * this program add two integer and
 * returns the sum
 */
#include "main.h"
#include <stdio.h>
/**
 * add- this function adds and
 * return the sum value
 * @x: 1st int parameter
 * @y: 2nd int parameter
 * Return: the sum of parameters
 */
int add(int x, int y)
{
	int sum;

	sum = (x + y);
	return (sum);
}
